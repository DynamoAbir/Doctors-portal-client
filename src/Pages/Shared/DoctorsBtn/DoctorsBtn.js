import React from 'react';

const DoctorsBtn = ({ children }) => {
    return (
        <button className="btn border-none bg-gradient-to-r from-[#0FCFEC] to-[#19D3AE]">{children}</button>
    );
};

export default DoctorsBtn;